#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int** Element_liste;

void init();
int suche(int x, int y);
void listevergroessern();
void einfuegen(int x, int y);
int suche(int x, int y);
